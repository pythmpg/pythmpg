=============
Miscellaneous
=============

Does Trey's name still need to be added to the collaborator/author
list in some places?

Overleaf
--------

Finish the document on Overleaf.  I think the main item that
is missing is the installation instructions, but check over
everything else.

Fig. 1 has been replaced with a snapshot of the latest version,
and Fig. 2 shows the same after some sorting operations.  OK
now I think.

Instructions for creating the Excel spreadsheet from the template
and .cvs file are now in place, but add comments for the parallel
process using LibreOffice?

Install the PDF in the git repository.  Where should it go?
Maybe below docs/ somewhere, so that the API has access to it?

Zenodo
------

What should be stored at Zenodo?  Is it usual to store a .zip of
the entire git repository?  If so, it seems to me we also want to
have a .zip similar to the one that is already there, with just
the spreadsheets and the User Guide, for users who don't need to
use the python code.

ReadTheDocs
-----------

I (DV) set up an account linked to my Github account, but I
can't created the RTD site for pythmpg until the git repository
is public.

==============
Git Repository
==============

Examples directory
------------------

Make sure everything is consistent.

Double-check that the .xlsx spreadsheets show the same data
as before.

Should this directory be deeper in the file tree, under
docs/ for example, so that the API knows about it?

Notes directory
---------------

Currently there is a notes/ directory containing DEV.md that
has been copied verbatim from the pythtb repo.  Just remove it?
Or modify/simplify it?

Miscellaneous
-------------

The PDF version of the User Guide from Overleaf needs to go
somewhere.  Just place it as MPG-User-Guide.pdf at the top
level?  Or should it go deeper in the file tree?

READ.md currently refers to an issues/ subdirectory that does
not currently exist.  Either remove that reference, or add the
directory.

=============================
Last-minute on day of release
=============================

Adjust the date in the CITATION.cff file.
