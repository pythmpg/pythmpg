===============
The python code
===============

I haven't heard any complaints, so unless changes are proposed
soon, I think we can consider the code itself to be finalized.

Documentation: As a reminder, it is sitting temporarily at
  file:///home/dhv/menus/hosted.html
However, it will move to ReadTheDocs when the release is done.

======================================
Example spreadsheet generation scripts
======================================

So far there are just the ones that reproduce the ones from
Version 0.0.0.  Add some more?

(AU) I see mpg_jahn_test now. Is it a new one? It looks good 
to me and it is a relatively general test, I think it should 
suffice (together with the other examples that were there 
already, i.e., mpg_min_bool, mpg_min_num, and mpg_exten. 

===================
Overleaf User Guide
===================

Tentatively finished.

Check that the installation instructions (via pip) are correct.

(AU) It looks ok to me -- I guess it will work after the release, right?

It may be good for someone to try the csv -> xlsx cut-and-paste
instructions and see if anything is wrong or unclear.

It may be a good idea to do the same with LibreOffice and
Numbers, and add comments as footnotes for hints for those
apps.

Install the PDF in the git repository.  Where should it go?
Maybe below docs/ somewhere, so that the API has access to it?

Also install the PDF directly in Zenodo.  I put it both as a file
at the top level, and inside a .zip file with the example
spreadsheets.

======
Zenodo
======

I tentatively decided to store three files:

- The User Guide PDF
- A .zip of the User Guide and example .xlsx spreadsheets
- A .zip of the entire git repo

OK?  Everything is now there, but needs to be updated before
any release.

ReadTheDocs
-----------

I (DV) set up an account linked to my Github account, but I
can't created the RTD site for pythmpg until the git repository
is public.  So this will have to be done last minute.

==============
Git Repository
==============

Here is the current layout via 'tree -L 2', ignoring .git* :

├── CITATION.cff
├── LICENSE
├── README.md
├── TODO.txt
├── docs
│   ├── Makefile
│   ├── build
│   ├── make.bat
│   └── source
├── examples
│   ├── MPG-exten.xlsx
│   ├── MPG-min-bool.xlsx
│   ├── MPG-min-num.xlsx
│   ├── MPG-template.xlsx
│   ├── mpg_exten.csv
│   ├── mpg_exten.out
│   ├── mpg_exten.py
│   ├── mpg_jahn_test.csv
│   ├── mpg_jahn_test.out
│   ├── mpg_jahn_test.py
│   ├── mpg_min.out
│   ├── mpg_min.py
│   ├── mpg_min_bool.csv
│   ├── mpg_min_num.csv
│   └── setup
├── notes
│   └── DEV.md
├── pyproject.toml
├── pythmpg
│   ├── __init__.py
│   ├── __pycache__
│   ├── mpg_dicts.py
│   ├── mpg_tools.py
│   ├── parse_jahn.py
│   ├── pg_elements.py
│   └── spreadsheet.py
└── readthedocs.yaml

The file you are reading is TODO.txt.  It should be removed
before release.

Where should the User Guide PDF go?  At the top level?  Or
inside 'docs/' so that the documentation can serve it?

README.md currently refers to an issues/ subdirectory that does
not currently exist.  Either remove that reference, or add the
directory and set things up for it.

The README.md currently has a kind of menu bar near the top with
links to a DOI for Zenodo and a 'docs' that will hopefully work
after ReadTheDocs is set up.

Examples directory
------------------

Make sure everything is consistent.

Double-check that the .xlsx spreadsheets show the same data
as before.

Should this directory be deeper in the file tree, under
docs/ for example, so that the API knows about it?

Notes directory
---------------

Currently there is a notes/ directory containing DEV.md that
has been copied verbatim from the pythtb repo.  Just remove it?
Or modify/simplify it?

Miscellaneous
-------------

=============================
Last-minute on day of release
=============================

Here is the way I currently see it.  What am I missing?  Is
the right order of operations?

  Adjust the date in the CITATION.cff file.

  Remove TODO.txt.

  Finalize the git repository, and make it public.

    As I understand, this is not the same as releasing it.

  Create the ReadTheDocs documentation site.

    If this requires any changes to the git repo, make them now.

    Make sure that the links in the User Guide and in the git
    repo point to the correct ReadTheDocs URL.

  Release and publish 1.0.0 on Github

  Capture the entire git repo in a .zip file and copy it to Zenodo.

  Go to Zenodo, fix any final things, and release 1.0.0 there.
